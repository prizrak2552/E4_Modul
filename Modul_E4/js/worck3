'use strict'
console.clear()

console.info(`
Задание 3.

Написать функцию, которая создает пустой объект, но без прототипа.
`)

const noProto = () => {
  return Object.create(null)
}

console.log(noProto())